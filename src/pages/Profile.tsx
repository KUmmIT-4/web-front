// import React, { useState } from "react";
import Selected from "../components/selected";

const Profile = () => {
  return (
    <div>
      <title>Profile</title>
      <Selected />
    </div>
  );
};

export default Profile;
